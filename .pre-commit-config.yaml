repos:
- repo: local
  hooks:
    - id: llmkg-backend-lint-pytest
      name: Run LLMKG Backend python lint and pytest
      entry: bash -c 'cd backend && make pre-commit'
      language: system
      pass_filenames: false
